using System;

namespace Conditionals
{
    class Program
    {
        static void Main(string[] args)
        {
            var number = 200;
            //switch (number)
            //{
            //    case 10:
            //        Console.WriteLine("Number is 10");
            //        break;
            //    case 20:
            //        Console.WriteLine("Number is 20");
            //        break;
            //    default:
            //        Console.WriteLine("Number is not 10 or 20");
            //        break;
            //}
            //if (number >= 0 && number <= 100)
            //{
            //    Console.WriteLine("Numara 0 ile 100 arasında");

            //}
            //else if (number > 100 && number <= 200)
            //{
            //    Console.WriteLine("Numara 101 ile 200 arasında");
            //}
            //else if (number > 200 || number < 0)
            //{
            //    Console.WriteLine("Numara 0 ile 200 arasında");
            //}

            if (number < 100)
            {
                if (number >= 90 && number < 95)
                {

                }
            }

            Console.ReadLine();
            Geridon sure = new Geridon();

        }
    }
}
